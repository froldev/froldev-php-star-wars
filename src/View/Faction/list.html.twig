{% extends 'layout.html.twig' %}

{% block title %}Star Wars - les Factions{% endblock %}

{% block content %}
    <div class="row">
        <h1 class="col-12">les factions</h1>
    </div>
    <nav class="row">
        <a href="/faction/add" class="btn sentinel" >ajouter une faction</a>
    </nav>
    <div class="row detail">
        {% for faction in factions %}
            <div class="col-md-4 col-sm-12 text-center card-view">
                <a href="/faction/details/{{faction.id}}">
                    {% if faction.picture %}
                        <img src="{{faction.picture}}" alt="{{faction.name}}" border="0" />
                    {% else %}
                        <img src="{{noPicture}}" alt="Pas d'image" border="0"  />
                    {% endif %}
                    <div class="card-img-overlay d-flex flex-column justify-content-end">
                        <h5>{{faction.name}}</h5>
                    </div>
                </a>
            </div>
        {% else %}
            <div class="col-12 text-center">Tu préfères maitriser la Force ou alors basculer du côté obscur de la Force ? 
            Sinon tu peux continuer ton chemin....</div>
        {% endfor %}
    </div>
{% endblock %}
